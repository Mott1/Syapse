class LogoGrid {

	constructor(target, index) {

		this.initVars(target, index);
		this.initEvents();

	}

	initVars(target, index) {

		const _this = this;

		// this.target = target.querySelector('.logos-container');

		// this.logoGridDraggable = Draggable.create(this.target, {type:"scrollLeft", edgeResistance:0.5, throwProps:true});

	}

	initEvents() {

		const _this = this;

	}

	stopEvents() {

		const _this = this;

	}

	// setEnable() {

	// 	this.logoGridDraggable[0].enable();

	// }

	// setDisable() {

	// 	this.logoGridDraggable[0].disable();

	// }

}